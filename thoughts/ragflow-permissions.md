# 2026-06-17: RAGFlow 的权限怎么设

第二个卡住的点：权限。

RAGFlow 的权限模型和我想的不太一样。知识库、文档、API key 各有一套控制逻辑，不是简单的「管理员/普通用户」二分。翻了文档也没找到一篇讲清楚的。

几个让我困惑的地方：

- 知识库的可见性——创建者、团队成员、所有人，这三档之间到底差在哪
- API key 的权限范围——它绑定的是用户还是知识库？如果一个人有多个知识库，key 能区分吗
- 如果我想让同事只能查不能改，能做到什么粒度

感觉 RAGFlow 的权限设计是为了团队协作场景做的，但文档没跟上。得自己一个个试。
