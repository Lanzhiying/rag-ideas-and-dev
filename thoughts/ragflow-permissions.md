# 2026-06-17: RAGFlow 的权限怎么设

第二个卡住的点：权限。

一个大文件夹十几个子文件夹，不同的人该看不同的。一开始想的是直接在知识库上配团队——但管理十几个知识库的团队设置太麻烦了，增减一个人要改好多地方。

换了个思路：不直接暴露知识库，用智能体做中间层。

建三四个智能体，每个智能体绑一组知识库。公共用户的智能体只能搜公开库，内部同事的智能体多绑几个库。用户不碰知识库，只跟智能体对话。

这个方案的关键是 RAGFlow 的智能体和知识库之间是不是真绑定——智能体能搜到没绑的库吗？测一下就知道了。

比直接管十几个知识库的权限干净多了。思路是对的，等验证。
